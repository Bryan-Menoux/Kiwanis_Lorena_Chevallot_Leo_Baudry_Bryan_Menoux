---
import { getImage } from "astro:assets";
import type { ImageMetadata } from "astro";

import { resolveHeroVariants } from "../utils/imageOptimization";

interface Props {
  image: ImageMetadata;
  title: string;
  subtitle: string;
  imageAlt?: string;
  loading?: "lazy" | "eager";
  fetchpriority?: "high" | "low" | "auto";
}

const {
  image,
  title,
  subtitle,
  imageAlt = "",
  loading = "eager",
  fetchpriority = "high",
} = Astro.props as Props;
const { desktop, tablet, mobile } = resolveHeroVariants(image);

const desktopImageMd = await getImage({
  src: desktop,
  format: "webp",
  width: 1440,
  quality: 80,
});
const desktopImageLg = await getImage({
  src: desktop,
  format: "webp",
  width: 1840,
  quality: 80,
});

const tabletImage = tablet
  ? await getImage({
      src: tablet,
      format: "webp",
      width: 1280,
      quality: 72,
    })
  : null;

const mobileImage = mobile
  ? await getImage({
      src: mobile,
      format: "webp",
      width: 440,
      quality: 68,
    })
  : null;

const tabletSrcSet = tabletImage?.srcSet.attribute || tabletImage?.src;
const mobileSrcSet = mobileImage?.srcSet.attribute || mobileImage?.src;
---

<div class="relative w-[95dvw] h-[85dvh] mx-auto rounded-box mt-[10dvh] lg:mt-28 flex flex-col justify-end px-8 md:px-12 lg:px-16 py-12 md:py-20 lg:py-44 overflow-hidden">
  <picture class="absolute inset-0">
    {
      mobileSrcSet && (
        <source
          media="(max-width: 767px)"
          srcset={mobileSrcSet}
          sizes="95vw"
          type="image/webp"
        />
      )
    }
    {
      tabletSrcSet && (
        <source
          media="(min-width: 768px) and (max-width: 1023px)"
          srcset={tabletSrcSet}
          sizes="95vw"
          type="image/webp"
        />
      )
    }
    <img
      src={desktopImageLg.src}
      srcset={`${desktopImageMd.src} 1440w, ${desktopImageLg.src} 1840w`}
      sizes="95vw"
      width={desktopImageLg.attributes.width}
      height={desktopImageLg.attributes.height}
      alt={imageAlt}
      loading={loading}
      decoding="sync"
      fetchpriority={fetchpriority}
      class="h-full w-full"
      style="height:100%;width:100%;object-fit:cover;object-position:center;"
    />
  </picture>

  <div class="absolute inset-0 bg-linear-to-t from-black via-black/0 to-transparent" aria-hidden="true"></div>

  <h1 class="relative z-10 text-white text-4xl! md:text-5xl! lg:text-7xl! font-bold">
    {title}
  </h1>
  <p class="relative z-10 text-white text-lg! md:text-xl! lg:text-2xl! font-medium">
    {subtitle}
  </p>
</div>
